package ontologies;


import agent.Agent;
import agent.Supervisor;

public class TechnologyCard extends Agent {
    public TechnologyCard(int id, Supervisor supervisor) {
        super(id, supervisor);
    }
}
